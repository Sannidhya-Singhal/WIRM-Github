import { AutoExpandTextAreaDirective } from './auto-expand-text-area.directive';

describe('AutoExpandTextAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoExpandTextAreaDirective();
    expect(directive).toBeTruthy();
  });
});
